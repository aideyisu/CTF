前期知识储备：
  SQL执行处理字符串的时候，字符串末尾的空格会被删除。 "Web_SQL" == "Web_SQL    "
  在所有的INSERT查询中，SQL都会根据varchar(n)来限制字符串的最大长度

因此：
  当"admin"账户存在的时候，我们可以注册"admin        "账户，并通过自己的密码来登陆"admin"账户

总结：
  在开发期间可以对唯一性的列加上UNIQUE约束。不能插入另一条记录
  同时最好使用'id'作为数据库表的主键。并且数据应该通过程序中的id进行跟踪
